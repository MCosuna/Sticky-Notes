class Notes {
  constructor(width, height, color, texto) {
    this.width = width;
    this.height = height;
    this.color = color;
    this.texto = texto;

    this.nota = createElement("textArea");
    // this.nota.style.
  }
}